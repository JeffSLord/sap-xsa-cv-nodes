ID: new-cv-nodes
_schema-version: '2.1'
version: 0.0.1

modules:
 - name: new_nodes_db
   type: hdb
   path: new_nodes_db
   requires:
    - name: hdi_new_nodes_db

resources:
 - name: hdi_new_nodes_db
   parameters:
      config:
         schema: NEW_NODES_DB
   properties:
      hdi-container-name: ${service-name}
   type: com.sap.xs.hdi-container
